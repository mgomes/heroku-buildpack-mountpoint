#!/usr/bin/env bash

BIN_DIR=$(
  cd "$(dirname "$0")"
  pwd
) # absolute path

source "$BIN_DIR/util"

if [ -z "${MOUNTPOINT_S3_BUCKET}" ] && [ -z "${MOUNTPOINT_PATH}" ]; then
  echo "MOUNTPOINT_S3_BUCKET NOT detected"
  exit 1
else
  echo "MOUNTPOINT_S3_BUCKET detected"
  exit 0
fi
